"""Content cleaning helpers.

HTML cleanup currently lives in `src.scraper` because Zendesk article bodies
are converted there directly. This module is reserved for shared cleanup logic
if more source types are added.
"""
